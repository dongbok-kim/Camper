<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.camper.mypage.dao.MyCampingLikeDAO">

	<!-- 캠핑장 찜 목록 -->
	<!-- by.승진 2022-08-08 -->
	<select id="myCampingLikeList" resultType="mypgLike" parameterType="String">
		SELECT ccl.cl_idx, ccl.cl_datetime, b.ca_name FROM cp_camping_like ccl, cp_camping b WHERE ccl.mb_id=#{param1} AND ccl.ca_idx= b.ca_idx ORDER BY cl_idx DESC
	</select>
	
</mapper>